#!/usr/bin/env node
/**
 * ARCHIVED — one-shot Firebase cleanup helper v2. No longer needed.
 */
console.error('[ARCHIVED] cleanup-firebase-v2.js — Firebase already removed from storage layers.');
process.exit(1);
