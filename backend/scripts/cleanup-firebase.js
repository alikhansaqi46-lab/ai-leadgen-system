#!/usr/bin/env node
/**
 * ARCHIVED — one-shot Firebase cleanup helper. No longer needed.
 */
console.error('[ARCHIVED] cleanup-firebase.js — Firebase already removed from storage layers.');
process.exit(1);
