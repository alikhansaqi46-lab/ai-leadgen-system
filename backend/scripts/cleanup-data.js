#!/usr/bin/env node
/**
 * ARCHIVED — Firebase/Firestore removed from LeadFlow AI runtime.
 * For a fresh local state, reset JSON under backend/data/ or truncate Postgres tables intentionally.
 */
console.error('[ARCHIVED] cleanup-data.js — Firebase removed. Reset backend/data/*.json or truncate Postgres manually.');
process.exit(1);
